import { html, LitElement } from "lit";
import { customElement } from "lit/decorators.js"

@customElement("benchmark-graph-viewer")
class BenchmarkGraphViewer extends LitElement {
    render() {
        return html`TODO: GRAPH`
    }

}